const state={data:null,completed:new Set(JSON.parse(localStorage.getItem('oa-case-studies-calendar-spreads-completed')||'[]'))};const $=s=>document.querySelector(s),list=$('#syllabusList');
async function loadSourceData(){const r=await fetch('../course-data.json');if(!r.ok)throw new Error('course data');return r.json();}
async function init(){state.data=await loadSourceData();renderSyllabus();loadLesson();$('#markComplete').onclick=completeCurrent;}
function renderSyllabus(){const l=state.data.lessons[0];list.innerHTML=`<section class="syllabus-section"><h3>${esc(l.section)}</h3><div class="syllabus-item active ${state.completed.has(1)?'done':''}" data-lesson="1"><span class="lesson-number">1</span><span class="lesson-check">${state.completed.has(1)?'✓':''}</span><div class="lesson-copy"><strong>${esc(l.title)}</strong><small>${esc(l.duration)}</small></div><span class="lesson-symbol">◷</span></div></section>`;updateProgress();}
function loadLesson(){const l=state.data.lessons[0],message=state.data.course.availabilityMessage;$('#lessonTitle').textContent=l.title;$('#lessonMeta').textContent='Lesson 1 of 1 · Reading';$('#aboutTitle').textContent=l.title;$('#aboutText').textContent=message;$('#availabilityText').textContent=message;$('#factSection').textContent=l.section;$('#prevBtn').disabled=true;$('#nextBtn').disabled=true;}
function completeCurrent(){state.completed.add(1);localStorage.setItem('oa-case-studies-calendar-spreads-completed',JSON.stringify([1]));const item=list.querySelector('[data-lesson="1"]');item.classList.add('done');item.querySelector('.lesson-check').textContent='✓';updateProgress();}
function updateProgress(){const done=state.completed.has(1)?1:0;$('#progressText').textContent=`${done} / 1`;$('#percentText').textContent=`${done?100:0}% complete`;$('#progressBar').style.width=`${done?100:0}%`;}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}init().catch(e=>{console.error(e);$('#lessonTitle').textContent='Could not load course data'});
